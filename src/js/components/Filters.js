import React from "react";

const Filters = () => (
    <nav className="navbar navbar-fixed-top navbar-secondary mb0">
        <div className="container-fluid">
            Search, Group By, Sort By
        </div>
    </nav>
);

export default Filters;
